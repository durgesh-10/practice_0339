spring.application.name=EmployeeRecord

# Corrected the URL
spring.datasource.url=jdbc:mysql://localhost:3306/Towen?autoReconnect=true&useSSL=false&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true

# Remove semicolons after `username` and `password`
spring.datasource.username=root
spring.datasource.password=root

# Keep the driver-class-name as is
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate and JPA configurations
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
